git config pull.rebase true

common uses:
>gitch -f/b/o ..
git checkout -b feature/..
git checkout -b bugfix/..
git checkout -b opt/..

>gitb -d/D ...
git branch -d ...
git branch -D ...

>gitch ...
git checkout <branch>
git checkout <file>
git checkout <branch> <file>

>configneeded>git pull
git pull origin development

git push
git push [-f]

git reset ...

>gitc ["..."]
git commit -m "..."
git commit --amend

git diff
git diff head
git diff <branch>
